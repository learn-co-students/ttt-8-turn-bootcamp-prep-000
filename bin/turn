#!/usr/bin/env ruby
require_relative '../lib/turn'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"

# move(board, 4, "X")
# move(board, 0, "O")
# move(board, 1, "X")

display_board(board)

turn(board)
# so excited!!!.... very close to complete the Game :)
# turn(board)
# turn(board)
# turn(board)
# turn(board)
# turn(board)
# turn(board)
# turn(board)
# turn(board)
